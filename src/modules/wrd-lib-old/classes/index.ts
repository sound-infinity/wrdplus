import SearchResults from './search-results'
import ThreadData from './thread-data'
import User from './user'

export default { SearchResults, ThreadData, User }
export { SearchResults, ThreadData, User }